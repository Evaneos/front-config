{
  "name": "front-config",
  "private": true,
  "engines": {
    "node": ">=v18",
    "npm": ">=9"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna publish --force-publish"
  },
  "devDependencies": {
    "lerna": "^7.4.1"
  }
}
